# 09 — 界面详细设计

> 上级文档：[DESIGN.md](../DESIGN.md) · 关联：[01 架构](./01-architecture.md)（事件驱动渲染） · [06 权限](./06-permissions.md)（审批卡片语义）
> 借鉴来源：OpenWebUI 的消息树 UI / 触发符 / 流式渲染细节；Claude Code Desktop / CodeX 的工具卡片密度

---

## 1. 设计 Token

```css
/* Tailwind v4 @theme —— 中性暗底 + 琥珀强调色，区别于 ChatGPT 绿 / Claude 陶土橙 */
--color-bg:        #101014 / light: #fafafa;
--color-surface:   #18181d / light: #ffffff;
--color-surface-2: #202028 / light: #f2f2f4;   /* 卡片、输入框 */
--color-border:    #2a2a33 / light: #e4e4e9;
--color-text:      #e8e8ed / light: #1a1a1f;
--color-text-dim:  #8b8b96 / light: #6b6b76;
--color-accent:    #f5a623;                     /* 琥珀：主按钮、活跃态、流式光标 */
--color-ok: #34c77b; --color-warn: #f5a623; --color-danger: #f0524f;
--color-agent:     #6c8cff;                     /* 工具/Agent 活动专用色（与 accent 区分）*/

--font-ui:   Inter, "PingFang SC", system-ui;
--font-mono: "JetBrains Mono", ui-monospace;    /* 工具参数、代码、ref、成本数字 */

--radius-card: 10px; --radius-input: 12px; --radius-chip: 999px;
--space-unit: 4px;   /* 间距全为 4 的倍数；消息纵距 16px，工具卡片纵距 8px（紧凑）*/
--text-body: 14px/1.6;  --text-tool: 12.5px;  --text-meta: 11px;
```

- 主题：跟随系统 + 手动三态；两套色板同一变量名，组件零感知。
- 动效克制：流式光标（accent 方块闪烁）、工具卡片状态色过渡 150ms、审批卡片入场 slide-in 200ms。禁用装饰性动画。

## 2. 组件层级树（两形态共享核心）

```
<ThreadView>                        ← 侧边栏与全屏页共用
 ├─ <MessageStream>                 虚拟滚动（长会话）
 │   ├─ <UserMessage>               编辑重发入口 → 分叉
 │   ├─ <AssistantMessage>
 │   │   ├─ <MarkdownRenderer>      react-markdown + Shiki + KaTeX + Mermaid
 │   │   ├─ <ReasoningBlock>        折叠，流式时显示"思考中"摘要行
 │   │   └─ <BranchSwitcher>        ‹ 2/3 ›（siblings 派生，见 02 §3.2）
 │   ├─ <ToolCallGroup>             连续工具调用折叠组
 │   │   └─ <ToolCallCard>          一行摘要 + 可展开详情
 │   │       └─ <SnapshotViewer>/<ScreenshotViewer>/<DiffViewer>   details 通道渲染
 │   ├─ <ApprovalCard>              审批 RPC 的 UI 端
 │   └─ <SystemNotice>              暂停/压缩/软提醒等浅色横条
 ├─ <PromptInput>
 │   ├─ <TriggerMenu>               @ / 斜杠 统一建议框架
 │   ├─ <AttachmentTray>  <ContextChips>   已附着的页面/选区/文件胶囊
 │   └─ <InputToolbar>              模型选择器 · 工具级别开关 · 成本角标 · 发送/Stop
 └─ <TaskPanel>                     todo 列表 + 上下文% + 成本 + 受控 tabs（全屏页右栏 / 侧边栏折叠条）
```

## 3. 页面线框

### 3.1 全屏对话页（三栏，中栏 720px 上限居中）

```
┌─────────────┬──────────────────────────────────────────────┬──────────────┐
│ ⌕ 搜索会话    │  耳机调研对比                    claude-sonnet-5 │ 任务 ▾        │
│ ✚ 新会话     │ ──────────────────────────────────────────── │ ☑ 打开商品页A  │
│─────────────│  ◆ 帮我调研这三款耳机的评价并做对比表              │ ☑ 读取评价     │
│ 📁 购物研究   │                                              │ ◻ 商品页B     │
│   ● 耳机调研  │  ⏺ 我将依次打开三个商品页读取评价…                │ ◻ 汇总对比     │
│   翻译PDF    │  ▸ 🌐 打开标签页 taobao.com/item…       ✓ 1.2s │──────────────│
│ 今天         │  ▸ 📄 读取页面  (增量快照 s4, 812 tok)   ✓      │ 上下文 ▓▓░ 42% │
│   周报生成    │  ▾ 6 步浏览器操作 ✓✓✓✓✓⏳  [展开]               │ $0.18 · 31k tok│
│ 昨天         │                                              │──────────────│
│   …         │  ⚠ 允许在 taobao.com 点击 [加入购物车] ?          │ 受控标签页     │
│             │    参数: ref=s4_17 "加入购物车"                  │  淘宝-商品A ↗  │
│             │    [允许一次·Y] [本站始终·A] [拒绝·N]             │  京东-商品B ↗  │
│─────────────│ ──────────────────────────────────────────── │              │
│ ⚙ 设置       │  ⌨ @引用 /命令…            [claude-s5▾][🌐▾][➤] │              │
└─────────────┴──────────────────────────────────────────────┴──────────────┘
```

左栏：搜索（标题+全文，Dexie 索引）、文件夹（可折叠、绑定 preset）、时间分组、置顶/未读点；右键菜单：改名/置顶/移动/归档/导出/删除。右栏 `<TaskPanel>` 可整体折叠。

### 3.2 侧边栏（360–500px）

```
┌────────────────────────────┐
│ 耳机调研 ▾         ⛶  ✚  ⋯ │   ← 会话下拉 / 展开全屏(定位当前会话) / 新会话
│ ▸ 任务 2/4 · 42% · $0.18   │   ← TaskPanel 折叠条，点开下拉
│────────────────────────────│
│ 📎 当前页: 淘宝-XX耳机  [＋]  │   ← 上下文胶囊：默认不注入，点＋附着
│────────────────────────────│
│      (MessageStream 同款)    │
│────────────────────────────│
│ ⌨ 问问当前页面…        [➤]  │
│ claude-sonnet-5 ▾  🌐▾  ⏹  │
└────────────────────────────┘
```

### 3.3 首次引导（Onboarding，全屏页首启）

三步卡片流：① 选预置模板填 API Key（内联 Verify，成功即亮绿）→ ② 选默认审批档（推荐 untrusted，一句话解释两轴）→ ③ 演示卡（"试试：@当前页面 总结一下"）。可跳过，未配置 Provider 时输入框置灰并引导。

### 3.4 设置页

左侧竖向 nav：`通用 / Providers / 模型预设 / 浏览器权限 / Skills / Plugins / MCP 服务器 / 数据 / 关于`。关键版式：

- **Providers**：连接卡片列表（状态点：绿=verified/灰=disabled/红=error）→ 点入编辑表单（baseUrl/keys 多行/自定义头 kv 表/quirks 折叠区/[Verify] 按钮及结构化结果）；
- **浏览器权限**：两轴默认档选择器（带语义说明）+ 规则表（列：工具/站点/裁决/来源/操作），来源为审批的可跳回原会话；敏感站点黑名单单独区块；
- **MCP**：服务器卡片（状态机指示 + 工具数 + [日志] 抽屉）+ [粘贴 JSON 导入]。

## 4. 关键交互状态机

### 4.1 流式渲染

```
idle → streaming(item.delta 追加)
  规则1: 未闭合代码块 —— 检测 ``` 奇偶，未闭合时以纯 pre 渲染，闭合后才交给 Shiki/Mermaid（防闪烁报错，OpenWebUI 教训）
  规则2: Mermaid/KaTeX 仅在块完整后渲染，失败降级为代码块
  规则3: 自动滚动 —— 用户上滚即解除跟随，出现 [↓ 回到底部] 胶囊
→ complete(终稿替换) | error(局部保留 + 重试按钮)
```

### 4.2 工具卡片

```
pending(参数已知, ⏳) → running(onUpdate 进度文本) → ok(✓ + 耗时) | fail(✗ + 错误摘要)
折叠规则: 连续 ≥3 张卡片折叠为组头 "N 步浏览器操作 ✓m ✗k"；运行中的组自动展开尾部一张
展开态: 参数(mono 字体) / content 结果 / details 富渲染(快照高亮、截图縮略图点击放大)
```

### 4.3 审批卡片

```
出现: slide-in + 输入框上方置顶 + 声音可选
键盘: Y=允许一次  A=本站始终  N=拒绝  (焦点自动落卡片, Esc=拒绝并停止)
多个排队: 队列显示 "1/3"，逐张处理
超时(5min): 变灰 + "已超时按拒绝处理"
flags 渲染: cross_scope → 卡片顶部橙色横幅"⚠ 越出任务作用域"; sensitive_payload → 红色横幅
```

### 4.4 运行/停止

发送键 ➤ ↔ 运行时变 ⏹（Esc 同效）。运行中输入：`Enter`=插话 steer（不可插话轮自动降级排队并 toast 说明），`Shift+Alt+Enter`=显式排队；队列胶囊显示待发条数、可点开删改。

## 5. 触发符与动态变量

`<TriggerMenu>` 统一调度（模糊搜索、↑↓ 选择）：

| 触发 | 内容 |
|---|---|
| `@` | 当前页面正文 / 当前选中文本 / 截图当前页 / 打开的标签页列表 / MCP Resources / 已上传文件 |
| `/` | 内置命令 + Skill 命令 + MCP Prompts；带 variables 的弹结构化表单（08 §4） |
| `{{` | 动态变量自动补全：`{{PAGE_URL}} {{PAGE_TITLE}} {{SELECTION}} {{CLIPBOARD}} {{CURRENT_DATE}}`，提交时求值 |

附着的上下文以胶囊显示在输入框上方，可单独移除；页面正文胶囊显示预估 token。

## 6. 快捷键全表

| 键 | 作用 | 作用域 |
|---|---|---|
| `Alt+P` | 开/关侧边栏 | 全局(commands) |
| `Cmd/Ctrl+K` | 命令面板（切会话/改模型/命令） | 扩展页 |
| `Cmd/Ctrl+N` | 新会话 | 扩展页 |
| `Enter` / `Shift+Enter` | 发送 / 换行（可反转） | 输入框 |
| `Esc` | 停止 turn / 关面板 | 扩展页 |
| `Y / A / N` | 审批：一次/本站/拒绝 | 审批卡片聚焦时 |
| `Cmd/Ctrl+↑↓` | 分支切换 | 消息聚焦时 |
| `Cmd/Ctrl+E` | 侧边栏 ⇄ 全屏页切换 | 扩展页 |

## 7. 空态 / 错误态 / 加载态

- **空会话**：居中 logo + 4 张建议卡（结合当前页域名的场景建议 + Skill auto_suggest）；
- **Provider 未配置**：输入框占位 "先在设置中添加模型 →"，点击直达；
- **错误态规范**：ProviderError 按 03 §7 归因显示人话（"API Key 无效，请检查 [设置]"），可重试的带 [重试] 按钮；网络断开顶部细横幅；
- **加载态**：会话切换用骨架屏（3 条消息形状）；重连中输入框显示 "重新连接引擎…"（01 §3.4 握手期间）。

## 8. i18n 与可达性

- 文案全部 key 化（zh-CN / en 两包），默认跟随浏览器语言；
- 审批卡片、工具卡片有完整 aria 标注；全键盘可达是验收项（Tab 顺序：消息流 → 审批 → 输入框）；
- 颜色对比度 ≥ WCAG AA；状态不只靠颜色（✓/✗/⏳ 图标并存）。

## 9. 开放问题

- [ ] Mermaid 在暗色主题下的配色注入方案（init 主题变量）。
- [ ] 消息流虚拟滚动与流式追加的滚动锚定实现选型（react-virtuoso 优先验证）。
- [ ] 侧边栏 <400px 时 ToolCallCard 的降级布局（隐藏耗时，仅图标+状态）。
